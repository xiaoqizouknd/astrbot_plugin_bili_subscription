# B站订阅推送

自动监控 B 站账号的新视频、动态与专栏，推送到指定群聊。

## 功能

- **视频更新**：以视频形式直接发送到群
- **动态更新**：合成一张图片发送，再以合并转发形式发送原图
- **专栏更新**：同动态
- **两种过滤**：视频动态、空动态自动跳过，避免刷屏

## 依赖

- AstrBot `>=4.26,<5`
- Python `>=3.10`
- `ffmpeg`：用于视频封装，未安装时视频推送跳过
- `Pillow`：用于合成卡片，未安装时动态和专栏无法发送

```bash
python -m pip install Pillow